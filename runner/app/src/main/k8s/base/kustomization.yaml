apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml

# Generate ConfigMap from example workflow (shared across all variants)
configMapGenerator:
  - name: workflow-example
    files:
      - example-workflow.yaml
