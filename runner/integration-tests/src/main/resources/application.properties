# Quarkus Flow Runner Integration Tests Configuration

# Enable the runner
quarkus.flow.runner.enabled=true

# Use a random port to avoid conflicts during parallel test execution
quarkus.http.test-port=0
