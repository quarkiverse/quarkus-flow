name: Flow Runner
description: |
  API to enable running Quarkus Flow from HTTP endpoints
artifact: ${project.groupId}:${project.artifactId}:${project.version}
metadata:
  keywords:
    - workflow
    - workflows
    - cncf
    - serverless
    - cloudevents
    - http
    - runner
  guide: https://docs.quarkiverse.io/quarkus-flow/dev/runner.html
  categories:
    - "integration"
    - "web"
  status: "preview"
