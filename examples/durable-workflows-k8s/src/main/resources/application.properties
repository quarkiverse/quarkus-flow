quarkus.http.port=8080

# Required for quarkus-flow-durable-k8s schedulers
quarkus.scheduler.start-mode=forced

# Useful for kube probes
quarkus.smallrye-health.root-path=/q/health

# ---- Durable Kubernetes pool/lease settings (replace with your real keys) ----
# Example ideas (NOT authoritative):
quarkus.flow.durable.kube.controllers.pool-name=demo-flow

quarkus.log.category."io.quarkiverse.flow".level=DEBUG