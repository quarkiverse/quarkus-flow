# Test configuration for OIDC deployment tests
quarkus.flow.oidc.enabled=true

# Disable actual OIDC connections for tests (we're just testing config generation)
quarkus.oidc-client.devservices.enabled=false
