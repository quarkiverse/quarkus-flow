quarkus.http.test-port=0

# We are using WireMock
quarkus.langchain4j.ollama.devservices.enabled=false

# Disable scheduler by default so @ScheduleOn agents don't fire during unrelated tests.
# FlowScheduleOllamaMockResource re-enables it for SchedulableFlowsIT.
quarkus.scheduler.enabled=false
